# SPACBR — AUR packages
#
# Intentionally empty. Every package SPACBR currently depends on is in
# the official Arch repositories (see packages/base, packages/x11,
# packages/desktop, packages/hardware). Per §41-43 of CLAUDE.md: only
# add an entry here when a specific, justified need can't be met
# officially, and note that justification next to it. If this file
# ever gains an entry, the installer needs exactly one AUR helper
# (paru) to build it — don't add a second.
